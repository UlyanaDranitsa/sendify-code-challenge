[project]
name = "sendify-code-challenge"
version = "0.1.0"
description = "MCP server that retrieves and structures info from db-schenker tracking website (sendify code challenge)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.26.0",
    "playwright>=1.58.0",
    "pydantic>=2.12.5",
]
